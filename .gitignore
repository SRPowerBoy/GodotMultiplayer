# Godot editor cache / imported assets
.godot/
.import/
export.cfg
export_presets.cfg

# Collaboration plugin runtime data (per-machine, never shared)
.collab_backup/
.collabignore

# Godot 4.4+ resource UIDs are regenerated per machine
*.uid

# Built distribution artifact (attach to a Release instead of committing)
godot_collab_addon.zip

# Local development harness: the throwaway Godot project used to run the addon
# and its test suites. Not part of what users install.
project.godot
_test_*.gd

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/
